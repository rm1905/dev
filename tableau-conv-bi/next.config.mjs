/** @type {import('next').NextConfig} */
const nextConfig = {
  // Produces a self-contained .next/standalone build that's easy to run on the VM.
  output: "standalone",
  reactStrictMode: true,
};

export default nextConfig;
